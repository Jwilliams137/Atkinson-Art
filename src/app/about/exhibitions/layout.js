export const metadata = {
    title: "Exhibitions | Linda Atkinson",
    description: "Linda Atkinson's exhibitions.",
};

export default function ExhibitionsLayout({ children }) {
    return <>{children}</>;
} 